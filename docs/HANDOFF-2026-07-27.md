# Handoff — Desktop foundation delivery — 2026-07-27

The local desktop-foundation implementation has been delivered. Preserve the
full A01-A26 proof standard while finishing the remaining native, hardware,
signing, updater, and remote-CI receipts.

## Read first

- `docs/PLAN-DESKTOP-FOUNDATION.md` — stable A01–A26 requirement and proof map.
- `docs/BACKLOG.md`, “Desktop application foundation audit - 2026-07-26” —
  canonical requirements and linked S5/S6/D4/D6/D7/D8 work.
- `docs/DESKTOP-CHAOS-MATRIX.md` — executable local versus native chaos cases.
- `docs/DESKTOP-EXPERIENCE-BUDGETS.md` — numeric gates and still-missing device
  receipts; correct the debug-versus-installed overclaim before closure.
- `docs/DESKTOP-RELEASE.md` and `docs/BACKUP-RESTORE.md` — package, signing,
  updater, child-runtime, backup, and restore contracts.
- `docs/HANDOFF.md` — older product history; this dated handoff is authoritative
  for the active audit wave.

## Current state

- Delivery commit and synchronization details are recorded in the final
  session summary and `git log`; do not rely on the pre-delivery baseline
  `513ca2c`.
- Only `/home/caleb/projects/photoproof` is registered as a Git worktree.
  The fourteen fully merged `worktree-*` branches were deleted and worktree
  metadata was pruned.
- Local A09 health/recovery, A10 repair integrity, A14 disk/backup acceptance,
  and A19/A20 terminal events/backend-truth UI are now closed and green.
- Health now includes DB/schema, volume/archive/watcher inventory, retained
  timestamps, backend-authoritative root recovery, Restore Defaults actions,
  retained S5/relink repair reports, retryable integrity repair, and explicit
  fatal device-identity reset/relaunch.
- The NVIDIA package slice now has an explicit `nvidia-package` Cargo feature,
  strict Linux/x64 guard, runtime-contract staging/verification, package
  resolver, Tauri config, scripts, tests, and workflow. Default and
  `--features nvidia-package` Cargo checks pass. No real CUDA/TensorRT runtime
  has been staged or tested yet.
- Linux artifacts built from this tree:
  - `target/release/bundle/deb/Photoproof_0.1.0_amd64.deb`
  - `target/release/bundle/rpm/Photoproof-0.1.0-1.x86_64.rpm`
  - populated `target/release/bundle/appimage/Photoproof.AppDir`
- The extracted DEB installed smoke passed: bundled `pp-asr-server` adjacent to
  the app, schema 16, Usable in 7 ms, shutdown in 0 ms, nine backup files
  verified, restore succeeded, and rollback was retained. Receipt:
  `/tmp/photoproof-installed-smoke-RXFQh7/app-data/installed-smoke.json`.
  ASR Ready is false because no redistribution-safe pinned model fixture exists.
- Linux DEB, RPM, and AppImage all build. The DEB and real AppImage execution
  paths pass installed smoke at 7 ms to `Usable` and 0 ms shutdown, including
  adjacent ASR sidecar plus backup/restore helper proof. AppImage builds route
  through `run-tauri-bundle.mjs` with Linux `NO_STRIP=1` to avoid
  linuxdeploy's stale ELF stripper.

## Verified stopping-point gates

- Full Rust workspace: passed; desktop Rust library 290 passed, 3 ignored.
- Core library acceptance: 36 passed, 2 ignored.
- S5 orphan retention: 2 passed.
- Frontend: 100 files, 1,251 tests passed.
- Svelte/TypeScript check: 0 errors, 0 warnings.
- Desktop strict Clippy: passed with warnings denied.
- Default and `nvidia-package` Cargo checks: passed.
- NVIDIA contract tests: 4 passed; wiring verifier passed.
- Rust formatting and `git diff --check`: passed.
- Full A25 chaos matrix: all 15 suites green; 23 cases passed and 5 explicitly
  retained as platform drills.

## Prioritized remaining work

1. **Real NVIDIA package proof — A15/A23.**
   Supply `PHOTOPROOF_ORT_DYLIB` with CUDA provider bytes and optionally
   `PHOTOPROOF_TRT_LIBS`, run `bundle:nvidia`, then validate the installed
   contract and real provider execution on a labeled NVIDIA runner. Inspect
   `apps/desktop/scripts/nvidia-runtime.mjs`,
   `apps/desktop/src-tauri/src/ort_runtime.rs`, and
   `.github/workflows/desktop-nvidia-package.yml`.

2. **Installed child readiness — A21.**
   Provide a pinned redistribution-safe Parakeet model fixture and prove the
   clean installed artifact reaches ASR Ready without workspace/PATH help.
   Do not round up the existing placement-only receipt.

3. **Native/platform matrix — A04/A08/A12/A14/A22–A26/S6.**
   Run real unresponsive NAS, CPAL device removal, suspend/resume, two-webview,
   crash/relaunch, APFS case-only rename, macOS/Windows control-file drills,
   signed update/rollback, and representative SSD/removable/NAS/Apple/NVIDIA
   performance receipts.

4. **CI and release evidence — A24.**
   Run the checked-in three-OS workflows remotely. Workflow source and local
   YAML validation are not a remote CI receipt.

5. **Signed release and device evidence.**
   Provision the updater/signing services, run macOS and Windows installed
   package/update rollback drills, and retain representative SSD, removable,
   NAS, Apple, NVIDIA, and Windows performance receipts.

## Hard rules

- Do not mark the goal complete while any native/package/signing/provider proof
  is missing or indirect.
- Preserve authored journals, collections, sidecars, and database truth; only
  derived caches/vectors/models may be rebuilt.
- Installed releases must not discover `llama-server` from developer `PATH`.
- The staged fp16 CLIP model remains unoffered until it has immutable hosted
  bytes.
- Do not silently mint a replacement device identity; only the explicit fatal
  recovery action may do so.
- Do not overwrite or discard the current dirty worktree. Inspect overlapping
  edits before changing shared files.

## Conventions

- Search with `rg`/`rg --files`; edit with `apply_patch`.
- Format with `cargo fmt --all`; use strict `cargo clippy ... -- -D warnings`.
- Run Rust gates from the repository root and frontend gates from
  `apps/desktop`.
- Use `BUN_TMPDIR=/tmp` for bundle commands in this environment.
- Record exact artifact paths, commands, counts, ignored tests, and external
  blockers; source inspection is never a substitute for installed/native proof.
