{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeDevCommand": "npm run verify:nvidia-runtime && cargo build -p pp-asr-server --features engine-parakeet && npm run dev",
    "beforeBuildCommand": "npm run build:bundle:nvidia"
  },
  "bundle": {
    "externalBin": [
      "binaries/pp-asr-server"
    ],
    "resources": {
      "nvidia-runtime/": "runtime/"
    }
  }
}
